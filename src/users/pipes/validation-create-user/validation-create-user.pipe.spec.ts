import { ValidationCreateUserPipe } from './validation-create-user.pipe';

describe('ValidationCreateUserPipe', () => {
  it('should be defined', () => {
    expect(new ValidationCreateUserPipe()).toBeDefined();
  });
});
